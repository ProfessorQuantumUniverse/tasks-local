{
  "name": "tasks-selfhosted-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted backend for the Tasks app: passkey authentication and SQLite storage.",
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "cli": "node src/cli.js",
    "test": "node test/run.mjs"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/helmet": "^13.0.1",
    "@fastify/rate-limit": "^10.2.2",
    "@simplewebauthn/server": "^13.1.1",
    "better-sqlite3": "^11.9.1",
    "fastify": "^5.2.2"
  }
}
